language: php

php:
  - 7.1
  - 7.2
  - 7.3
  - 7.4

before_script:
  - composer install --prefer-source --no-interaction --dev --optimize-autoloader

script: XDEBUG_MODE=coverage vendor/bin/phpunit --bootstrap tests/bootstrap.php --configuration phpunit.xml  --coverage-clover coverage.xml tests

after_success:
  - bash <(curl -s https://codecov.io/bash)
